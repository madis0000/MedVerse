# Database
POSTGRES_DB=medpulse
POSTGRES_USER=medpulse
POSTGRES_PASSWORD=medpulse_secret
DATABASE_URL=postgresql://medpulse:medpulse_secret@localhost:5432/medpulse?schema=public

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=medpulse-jwt-secret-change-in-production
JWT_REFRESH_SECRET=medpulse-jwt-refresh-secret-change-in-production
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Backend
NODE_ENV=development
PORT=3001
CORS_ORIGIN=http://localhost:3000
UPLOAD_DIR=./uploads

# SMTP (Ethereal for dev)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@medpulse.com

# Frontend
VITE_API_URL=http://localhost:3001/api
